###General SQL Questions
#1. What does SQL stand for? 
--structured query language
#2. Which SQL statement is used to extract data from a database? 
--SELECT
#3. What does the WHERE operator do?
--filter date
#4. What is the difference between HAVING and WHERE clauses? 
--HAVING filters with agg functions
#5. What is GROUP BY used for? 
--to group
#6. What is ORDER BY used for? 
--sorting results/output
#7. What is an aggregate function and provide 3 examples? 
--calculations (on a column(s)), COUNT(), SUM(), AVG()
#8. What is the difference between AND or OR? 
--both used for filtering with WHERE; AND both conditions must be met; OR either/any condition must be met
#9. What is the difference between SELECT and SELECT DISTINCT? 
--DISTINCT returns unqiue values/rows (no duplicates)
#10. What is an alias? 
--using AS to rename column for the output
